#------------------------------------------------------------------------------
#
#                       Base configuration
#
#------------------------------------------------------------------------------

grid2.hub=http://

site.url=http://ellos.se/

browser.name=${browser.name}
browser.version=41.0.2
browser.platform=windows

wait.timeout=30

#${browser.name}